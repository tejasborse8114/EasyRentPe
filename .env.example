# Copy to .env.local (dev) or .env.production.local (VPS) — never commit real credentials.
# Hostinger mailbox SMTP credentials (hPanel → Emails → Connect Devices for exact host/port).
SMTP_HOST=smtp.hostinger.com
SMTP_PORT=465
SMTP_USER=support@easyrentpe.com
SMTP_PASS=your-mailbox-password
